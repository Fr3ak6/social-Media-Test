Per avviare correttamente il programma seguire i seguenti passaggi:
ps.(se si ha Windows come OS, avviare xampp e startare i servizi di apache e MySQL)


1) controllare la presenza del database, in caso di assenza eseguire la query presente sul file DB_Ercolani_Gian_Marco.sql

2) entrare nella directory relativa al progetto ed eseguire i seguenti comandi:
	-php artisan serve (Avvia un server di sviluppo integrato per l'applicazione Laravel.)
	-npm run dev (npm run dev generalmente avvia un server di sviluppo che permette di lavorare sulle componenti Vue in tempo reale,  avvia il server di sviluppo di Vite, che è veloce e 	 supporta il hot reloading, avvia il processo di build e il monitoraggio delle modifiche.)

3) aprire un qualsiasi browser e navigare verso la pagina [http://localhost:8000]

4) effettuare la registrazione per aver accesso al sito

